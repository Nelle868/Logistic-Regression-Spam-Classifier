# Logistic-Regression-Spam-Classifier-
Built a logistic regression classifier in Python and scikit-learn to distinguish spam from legitimate email. Engineered binary word features from raw text, evaluated with precision, recall, and false positive rate rather than accuracy alone, and analyzed the false positive versus false negative tradeoff in the filtering context.
